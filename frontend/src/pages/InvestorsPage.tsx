import { useEffect, useState } from "react"; import { InvestorForm } from "../components/InvestorForm"; import { Card } from "../components/ui/card"; import { Table, Td, Th } from "../components/ui/table"; import { api } from "../lib/api";
export function InvestorsPage(){const[items,setItems]=useState<any[]>([]); const load=async()=>{const r=await api.get('/investors'); setItems(r.data)}; useEffect(()=>{void load()},[]); return <section className="space-y-4"><h1 className="text-3xl font-bold text-brand-navy">Investidores</h1><Card><InvestorForm onSubmit={async d=>{await api.post('/investors',d); await load();}}/></Card><Card><Table><thead><tr><Th>Nome</Th><Th>Email</Th><Th>Telefone</Th><Th>Documento</Th></tr></thead><tbody>{items.map(i=><tr key={i.id}><Td>{i.name}</Td><Td>{i.email}</Td><Td>{i.phone}</Td><Td>{i.document}</Td></tr>)}</tbody></Table></Card></section>}