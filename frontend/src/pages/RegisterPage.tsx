import { useState } from "react"; import { Link, useNavigate } from "react-router-dom"; import { Button } from "../components/ui/button"; import { Card } from "../components/ui/card"; import { Input } from "../components/ui/input"; import { Label } from "../components/ui/label"; import { useAuth } from "../contexts/AuthContext";
export function RegisterPage(){const[name,setName]=useState(""),[email,setEmail]=useState(""),[password,setPassword]=useState(""); const {register}=useAuth(); const nav=useNavigate(); return <main className="grid min-h-screen place-items-center p-4"><Card className="w-full max-w-md"><h1 className="mb-4 text-2xl font-bold text-brand-navy">Cadastro</h1><form className="grid gap-4" onSubmit={async e=>{e.preventDefault(); await register(name,email,password); nav('/dashboard')}}><label><Label>Nome</Label><Input required value={name} onChange={e=>setName(e.target.value)}/></label><label><Label>Email</Label><Input required value={email} onChange={e=>setEmail(e.target.value)}/></label><label><Label>Senha</Label><Input required type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><Button>Cadastrar</Button><Link className="text-sm text-brand-green" to="/login">Ja tenho conta</Link></form></Card></main>}
