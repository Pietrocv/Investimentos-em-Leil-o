import { useState } from "react"; import { Link, useNavigate } from "react-router-dom"; import { Button } from "../components/ui/button"; import { Card } from "../components/ui/card"; import { Input } from "../components/ui/input"; import { Label } from "../components/ui/label"; import { useAuth } from "../contexts/AuthContext"; import logo from "../assets/logo.svg";
export function LoginPage(){const[email,setEmail]=useState("pietro@teste.com"),[password,setPassword]=useState("123456"),[err,setErr]=useState(""); const {login}=useAuth(); const nav=useNavigate(); return <main className="grid min-h-screen place-items-center bg-brand-bg p-4"><Card className="w-full max-w-md"><img src={logo} className="mb-6 w-full"/><form className="grid gap-4" onSubmit={async e=>{e.preventDefault(); try{await login(email,password); nav('/dashboard')}catch{setErr('Login invalido')}}}><label className="grid gap-1"><Label>Email</Label><Input value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="grid gap-1"><Label>Senha</Label><Input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{err&&<p className="text-sm text-red-600">{err}</p>}<Button>Entrar</Button><Link className="text-sm text-brand-green" to="/register">Criar conta</Link></form></Card></main>}
