import { Building2, LayoutDashboard, LogOut, Settings, Users } from "lucide-react"; import { NavLink } from "react-router-dom"; import { useAuth } from "../contexts/AuthContext"; import logo from "../assets/logo.svg";
const links = [{ to: "/dashboard", label: "Dashboard", icon: LayoutDashboard }, { to: "/properties", label: "Imoveis", icon: Building2 }, { to: "/investors", label: "Investidores", icon: Users }, { to: "/settings", label: "Configuracoes", icon: Settings }];
export function Sidebar() { const { logout } = useAuth(); return <aside className="fixed inset-y-0 left-0 z-20 hidden w-72 border-r bg-brand-navy p-5 text-white lg:block"><img src={logo} className="mb-8 rounded-md bg-white p-2"/><nav className="space-y-2">{links.map(l => <NavLink key={l.to} to={l.to} className={({isActive}) => `flex items-center gap-3 rounded-md px-3 py-2 text-sm ${isActive ? "bg-white text-brand-navy" : "text-slate-200 hover:bg-white/10"}`}><l.icon size={18}/>{l.label}</NavLink>)}</nav><button onClick={logout} className="absolute bottom-5 flex items-center gap-3 rounded-md px-3 py-2 text-sm text-slate-200 hover:bg-white/10"><LogOut size={18}/>Sair</button></aside>; }
