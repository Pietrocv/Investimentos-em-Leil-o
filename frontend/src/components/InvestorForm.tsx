import { useState } from "react"; import { Button } from "./ui/button"; import { Input } from "./ui/input"; import { Label } from "./ui/label"; import { Textarea } from "./ui/textarea";
export function InvestorForm({ initial={}, onSubmit }:{initial?:any;onSubmit:(d:any)=>Promise<void>}){const[f,setF]=useState<any>({name:"",email:"",phone:"",document:"",notes:"",...initial}); const ch=(k:string,v:string)=>setF((x:any)=>({...x,[k]:v})); return <form className="grid gap-3 md:grid-cols-2" onSubmit={async e=>{e.preventDefault(); await onSubmit(f);}}>{["name","email","phone","document"].map(k=><label key={k} className="grid gap-1"><Label>{k}</Label><Input required={k==="name"} value={f[k]||""} onChange={e=>ch(k,e.target.value)}/></label>)}<label className="grid gap-1 md:col-span-2"><Label>notes</Label><Textarea value={f.notes||""} onChange={e=>ch("notes",e.target.value)}/></label><Button className="w-fit">Salvar investidor</Button></form>}